# Memory Core Dependencies
# For full dependency management, use pyproject.toml
# Install with: pip install -e ".[dev,test]"

# Core dependencies (also in pyproject.toml)
mcp>=1.0.0
pyyaml>=6.0
numpy>=1.24.0

# Optional dependencies
# psycopg2-binary>=2.9.9  # For Postgres support
# cozo>=0.1.0  # For CozoDB support

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
